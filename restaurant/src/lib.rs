mod front_of_house;


fn serve_order() {}

fn eat_at_restaurant() {
    //Absolute path
    // crate::front_of_housing::hosting::add_to_waitlist();
    //Relative path

    let a =  crate::front_of_house::hosting::MAX;
}